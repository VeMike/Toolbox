<UserControl x:Class="Toolbox.UI.Viewer.PdfViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Toolbox.UI.Viewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.08*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- The name of the file beeing displayed -->
        <TextBox x:Name="pdfFileName" Grid.Column="0" Grid.Row="0" TextAlignment="Left" VerticalContentAlignment="Center" FontSize="16"/>
        <!-- Hosts the WindowsForms control, that displays the pdf file -->
        <WindowsFormsHost x:Name="windowsFormsHost" Grid.Column="0" Grid.Row="1"/>
    </Grid>
</UserControl>
